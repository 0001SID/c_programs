#include <stdio.h>
main()
{
    int c;
    c = getchar();
    while(c != EOF )
    {
        int a;
        a = EOF;
        putchar(a);
    c = getchar();
    }

} /* Has to modify it */
